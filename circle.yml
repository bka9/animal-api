machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t circleci/animalapi .

test:
  override:
    - docker run -d -p 3000:3000 circleci/animalapi sbt run; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:3000