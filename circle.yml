machine:
  services:
    - docker

dependencies:
  pre:
    - docker info
    - docker build -t bka9/animalapi:$CIRCLE_SHA1 .

test:
  override:
    - docker run -d -p 3000:3000 bka9/animalapi:$CIRCLE_SHA1 sbt run; sleep 15;curl -L --retry 10 --retry-delay 5 -v http://localhost:3000;
