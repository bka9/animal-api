machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t circleci/animalapi .

test:
  override:
    - docker run -d -P circleci/animalapi; sleep 30
    - curl --retry 10 -retry-delay 5 -v http://localhost:3000