machine:
  services:
    - docker

dependencies:
  pre:
    - docker info
    - docker build -t bka9/animalapi:$CIRCLE_SHA1 .

test:
  override:
    - docker run -d -p 8080:8080 bka9/animalapi:$CIRCLE_SHA1 sbt run; sleep 10;
    - curl --retry 10 --retry-delay 5 -v http://localhost:8080
